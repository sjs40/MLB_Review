Metadata-Version: 2.4
Name: batter-dashboard
Version: 0.1.0
Summary: MLB hitter game-level performance dashboard data layer
Requires-Python: >=3.11
Description-Content-Type: text/markdown
Requires-Dist: pybaseball==2.2.7
Requires-Dist: pandas==2.2.3
Requires-Dist: numpy==2.1.3
Requires-Dist: plotly==5.24.1
Requires-Dist: streamlit==1.40.2
Requires-Dist: requests==2.32.3
Requires-Dist: pyarrow==18.1.0
Provides-Extra: dev
Requires-Dist: pytest==8.3.4; extra == "dev"
Requires-Dist: ruff==0.8.4; extra == "dev"

# Batter Dashboard (Data Layer Scaffold)

This project is the foundational data layer for an MLB hitter game-level performance dashboard.

## Install

```bash
pip install -e .
```

## Smoke tests

```bash
python -m data.loader
python -m data.run_value_matrix
```

## Run tests

```bash
pytest
```

## Cache

Parquet cache files are written to `cache/` under the project root.

To clear cache:

```bash
rm -f cache/*.parquet
```
